# py_leda_vm

Python compiler and VM scaffolding for Leda.

Quick start:

1. cd py_leda_vm
2. python3 -m venv .venv && source .venv/bin/activate
3. python3 -m pip install -e .
4. python3 -m unittest discover -s tests -p 'test_*.py'
5. generate-goldens --help
